pipeline{
    agent any
     stages{
          stage('Git Clone'){
               steps{
                    sh 'sudo rm -rf *;sudo git clone ssh://github.com/sanjeevkumargm/hello-world'
               }
          }
     }
}
